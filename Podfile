# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
use_frameworks!

target 'underTaking' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for underTaking
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'GoogleSignIn'
  
  pod 'Clarifai'


  target 'underTakingTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'underTakingUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
